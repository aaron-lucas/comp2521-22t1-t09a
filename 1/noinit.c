#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int a, b, c, d, e;
    // a = b = c = d = e = 0;

    printf("a = %d, b = %d, c = %d, d = %d, e = %d\n", a, b, c, d, e);

    return EXIT_SUCCESS;
}

